name: Deploy

on:
  push:
    branches:
      - prod
      - test

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install
        run: yarn install

      - name: Deploy
        run: yarn deploy
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}